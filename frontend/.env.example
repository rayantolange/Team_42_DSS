# Base URL for the FastAPI backend. Defaults to "/api" if unset.
VITE_API_BASE_URL=http://localhost:8000/api

# Set to "false" once the team's FastAPI endpoints are live and you
# want the app to call the real backend instead of mock services.
VITE_USE_MOCK_API=true
